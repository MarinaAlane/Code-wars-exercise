// Achando agulha no palheiro 

// Escreva uma função findNeedle() que receba como parâmetro um array que tá cheio de tralha e tem
// uma agulha ('needle')

// A sua função tem que retornar a mensagem "found the needle at position " junto com a
// posição em que a agulha foi encontrada no palheiro que é um array.

function findNeedle(haystack) {
return "found the needle at position " + haystack.indexOf("needle");
  
}